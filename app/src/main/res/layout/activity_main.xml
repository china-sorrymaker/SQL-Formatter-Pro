<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/et_input_sql"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        # ❌ 原来：android:hint="Paste SQL here..."
        # ✅ 现在：引用多语言资源
        android:hint="@string/hint_sql"
        android:gravity="top"
        android:background="@android:drawable/editbox_background" />

    <Button
        android:id="@+id/btn_format"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_input_sql"
        android:layout_marginTop="16dp"
        # ❌ 原来：android:text="Format SQL"
        # ✅ 现在：引用多语言资源
        android:text="@string/btn_format" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/btn_format"
        android:layout_above="@+id/adView"
        android:layout_marginTop="16dp">
        <TextView
            android:id="@+id/tv_output_sql"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textIsSelectable="true"
            android:textColor="@android:color/black" />
    </ScrollView>

    <com.google.android.gms.ads.AdView
        android:id="@+id/adView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:adSize="BANNER"
        app:adUnitId="ca-app-pub-3940256099942544/6300978111" />

</RelativeLayout>
